<tables>
    <playerInfo>
        create table if not exists(
            id INT AUTO_INCREMENT PRIMARY KEY,
            column1 VARCHAR(255),
            column2 INT,
            column3 FLOAT
        )
    </playerInfo>
</tables>